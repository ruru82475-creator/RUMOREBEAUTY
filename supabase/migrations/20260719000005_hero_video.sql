-- ============================================================
-- 首頁 Hero 背景影片:由站主在後台單獨上傳指定,不再自動取精選作品
-- ============================================================

alter table public.profiles
  add column if not exists hero_video_url text;
